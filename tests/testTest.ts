
import 'jest';
import 'cahi';
import 'sinon';
import { expect } from 'chai';

describe('test', () => {
    test("test", async (done) => {
        // it("test", () => {
        //     expect("1").to.equal("1");
        // });

        expect();
        done();
    });
});